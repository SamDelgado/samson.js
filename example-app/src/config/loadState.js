/* Function that restores the last state of the app after a restart or resume from pause */

module.exports = function loadState(callback) {

  //App.Router.navigate('entry', 'fade');
  
  if (callback) callback();

};
